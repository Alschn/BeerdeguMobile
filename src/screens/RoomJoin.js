import { Text, View } from "react-native";

const RoomJoinScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Room Join screen</Text>
    </View>
  );
};

export default RoomJoinScreen;
