import { Text, View } from "react-native";

const PasswordChangeScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>PasswordChange screen</Text>
    </View>
  );
};

export default PasswordChangeScreen;
