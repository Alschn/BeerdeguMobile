import { Text, View } from "react-native";

const RoomCreateScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Room Create screen</Text>
    </View>
  );
};

export default RoomCreateScreen;
