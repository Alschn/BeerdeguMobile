import { Center, Text } from "native-base";

const StatisticsScreen = () => {
  return (
    <Center flex={1}>
      <Text fontSize="lg">Statistics screen</Text>
    </Center>
  );
};

export default StatisticsScreen;
